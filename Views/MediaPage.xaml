<Page x:Class="PersonalTracker.Media.Views.MediaPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      Style="{StaticResource PageStyle}"
      Title="Media Tracker"
      Loaded="Page_Loaded">
    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
        <StackPanel>
            <TextBlock Text="Media:" HorizontalAlignment="Center" Margin="0,0,0,10" />
            <Button x:Name="BtnBooks" Width="250" Content="_Books" Click="BtnBooks_Click" Margin="0,0,0,10" />
            <Button x:Name="BtnFilms" Width="250" Content="_Films" Click="BtnFilms_Click" Margin="0,0,0,10" />
            <Button x:Name="BtnMusic" Width="250" Content="_Music" Click="BtnMusic_Click" Margin="0,0,0,10" />
            <Button x:Name="BtnTelevision" Width="250" Content="_Television" Click="BtnTelevision_Click" Margin="0,0,0,10" />
        </StackPanel>
    </Grid>
</Page>